[project]
name = "crypto_analysis"
version = "0.1.0"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
include = ["database*", "dashboard*", "scripts*", "utils*", "services*", "indicators*", "backtesting*", "API*"]